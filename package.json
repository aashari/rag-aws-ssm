{
  "name": "rag-aws-ssm-command",
  "version": "1.2.3",
  "module": "src/index.ts",
  "type": "module",
  "private": false,
  "description": "A utility for executing shell commands and transferring files to AWS EC2 instances via SSM with enhanced output formatting, part of the RAG tool suite",
  "bin": {
    "rag-aws-ssm-command": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/progress": "^2.0.7"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.758.0",
    "commander": "^13.1.0",
    "progress": "^2.0.3"
  }
}